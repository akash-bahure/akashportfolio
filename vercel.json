{
    "version": 2,
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/static-build",
        "config": {
          "distDir": "dist"
        }
      },
      {
        "src": "backend/server.js",
        "use": "@vercel/node"
      }
    ],
    "routes": [
      { "src": "/api/(.*)", "dest": "/backend/server.js" },
      { "src": "/blog/(.*)", "dest": "../dist/index.html" },
      { "src": "/project/(.*)", "dest": "../dist/index.html" },
      { "src": "/allblogs", "dest": "../dist/index.html" },
      { "src": "/(.*)", "dest": "../dist/index.html" }
        
        
    ]
  }
  
